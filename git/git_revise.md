**To edit/revise commit msg.**

`git commit --allow-empty -m "Revise 'wrong-msg' to 'right-msg' Revise 986796fcf20e8d22a987546f8f463f3da7813dc"`

note: 'wrong-msg' doesn't have to be the whole commit msg, just a part of it.
