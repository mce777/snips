### Find the index of current element based on parentElement

```js
const index = [...el.parentElement.children].indexOf(el);

const index = [...entry.target.parentElement.children].indexOf(entry.target)
```
